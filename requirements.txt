# Agent service container deps. Pinned to known-good minimums; the
# evaluator runs `pip install -r requirements.txt` inside each container.

# LLM
litellm>=1.60.0

# Web service
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0

# Postgres — asyncpg for the FastAPI service, psycopg for ingest COPY.
asyncpg>=0.30.0
psycopg[binary]>=3.2.0

# Data + forecasting
pandas>=2.2.0
statsmodels>=0.14.0

# Env / config
python-dotenv>=1.0.0
